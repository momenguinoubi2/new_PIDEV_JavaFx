{% extends 'activities.html.twig' %}

{% block tbody %}
<br></br>
    <div class="container">
    <div class="row">
        <div class="col-md-3">
            {% include 'managment/_filtre.html.twig' with {form: form} only %}
        </div>
          <div class="col-md-9">
              <div class="d-flex justify-content-end mb-2">
                  {{ knp_pagination_sortable(produits, 'Prix', 'p.prix')}}
                  {{ knp_pagination_sortable(produits, 'Promo', 'p.promo')}}
              </div>
              <div class="grid">

                  {% for produit in produits %}
                      {% include 'managment/_card.html.twig' with {produit: produit} only %}

                  {% endfor %}

              </div>
              <div class=" d-flex justify-content-center mb-2">
{#                  {% do produits.setPageRange(2)%}#}
                  {{ knp_pagination_render(produits) }}
              </div>
          </div>

    </div>
    </div>
{% endblock %}
{#        <div class="col">#}
{#        <div class="card" style="width:300px">#}
{#            <img class="card-img-top" src="/uploads/images/{{produit.image}}" alt="Card image" style="width:100%">#}
{#            <div class="card-body">#}
{#                <h4 class="card-title">{{ produit.nom}}</h4>#}
{#                <p class="card-text">{{ produit.url }}</p>#}
{#                <a href="#" class="btn btn-primary">{{ produit.url }}</a>#}
{#            </div>#}